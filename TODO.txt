**General**
- make the a pop up dialog(the one in Activity 1) utlitze method in the helper
that take the msg and timer to notify the user with different things

- make a  validation methods for any gui inputs (strings, ints, doubles) like the terminal based 

- Find a better way for user enterance forms


- ------>>>>>>FIX THE LAYOUTS

- generate dummy rates and reviews in the generator class

**Activity 2**
- Verify the credientails of the user input from the gui (Verify the strings are valied, not the DB)

**Activity 3**
- Verify the credientails of the user input from the gui (Verify the strings are valied, not the DB)

